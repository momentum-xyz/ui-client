import styled from 'styled-components';
import {Text} from '@momentum-xyz/ui-kit';

export const Body = styled.div`
  display: flex;
  flex-direction: column;
  padding-bottom: 20px;
  width: 400px;
  gap: 10px;
`;

export const InitiativeDescription = styled(Text)`
  padding: 0 10px;
`;
