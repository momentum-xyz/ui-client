/**
 * Enum for staking reward destination type.
 * @readonly
 * @enum {string}
 */

export enum PayeeEnum {
  Staked = 'Staked',
  Stash = 'Stash',
  Controller = 'Controller',
  Account = 'Account'
}
